import { CapabilityApplication } from './capability-application';

describe('CapabilityApplication', () => {
  it('should create an instance', () => {
    expect(new CapabilityApplication()).toBeTruthy();
  });
});
