import { CapabilityInformation } from './capability-information';

describe('CapabilityInformation', () => {
  it('should create an instance', () => {
    expect(new CapabilityInformation()).toBeTruthy();
  });
});
